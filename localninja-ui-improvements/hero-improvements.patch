diff --git a/src/app/globals.css b/src/app/globals.css
index a6d4ce9..1b05c05 100644
--- a/src/app/globals.css
+++ b/src/app/globals.css
@@ -334,6 +334,12 @@ button:focus-visible,
   animation: float 4s ease-in-out infinite;
 }
 
+@media (prefers-reduced-motion: reduce) {
+  .floating-element {
+    animation: none;
+  }
+}
+
 .hero-grid {
   display: grid;
   grid-template-columns: 1fr 1.1fr;
@@ -436,6 +442,60 @@ button:focus-visible,
   border-radius: 0.5rem;
 }
 
+/* Hero trust row — social proof + risk-reversal beneath the CTAs */
+.hero-trust {
+  display: flex;
+  align-items: center;
+  flex-wrap: wrap;
+  gap: 0.75rem 1.75rem;
+  margin-top: 1.75rem;
+}
+
+.hero-trust__rating {
+  display: inline-flex;
+  align-items: center;
+  gap: 0.5rem;
+}
+
+.hero-trust__stars {
+  display: inline-flex;
+  color: #F59E0B;
+}
+
+.hero-trust__rating-text {
+  font-size: 0.9375rem;
+  color: var(--color-text-secondary);
+}
+
+.hero-trust__rating-text strong {
+  color: var(--color-text-dark);
+  font-weight: 700;
+}
+
+.hero-trust__points {
+  display: inline-flex;
+  flex-wrap: wrap;
+  align-items: center;
+  gap: 0.5rem 1.25rem;
+  list-style: none;
+  margin: 0;
+  padding: 0;
+}
+
+.hero-trust__points li {
+  display: inline-flex;
+  align-items: center;
+  gap: 0.375rem;
+  font-size: 0.9375rem;
+  font-weight: 500;
+  color: var(--color-text-secondary);
+}
+
+.hero-trust__points li svg {
+  color: var(--color-success);
+  flex-shrink: 0;
+}
+
 .hero-visual {
   display: flex;
   justify-content: center;
@@ -831,6 +891,7 @@ button:focus-visible,
   .hero h1 { font-size: 2.75rem; max-width: 100%; margin-left: auto; margin-right: auto; }
   .hero p { max-width: 600px; margin-left: auto; margin-right: auto; }
   .hero-buttons { justify-content: center; }
+  .hero-trust { justify-content: center; }
   .hero-visual { order: -1; }
   .hero-mockup-frame { max-width: 560px; transform: none; }
   .profile-grid { grid-template-columns: repeat(2, 1fr); }
diff --git a/src/components/HeroSection.jsx b/src/components/HeroSection.jsx
index 63e7de4..3635827 100644
--- a/src/components/HeroSection.jsx
+++ b/src/components/HeroSection.jsx
@@ -2,7 +2,7 @@
 
 import React from 'react';
 import Link from 'next/link';
-import { ArrowRight, TrendingUp, Star } from 'lucide-react';
+import { ArrowRight, TrendingUp, Star, Check } from 'lucide-react';
 import { useScrollReveal } from '@/hooks/useScrollReveal';
 import ProductMockup from './ProductMockup';
 
@@ -28,6 +28,25 @@ const HeroSection = () => {
               Explore Core Services
             </a>
           </div>
+
+          <div className="hero-trust">
+            <div className="hero-trust__rating">
+              <span className="hero-trust__stars" aria-hidden="true">
+                <Star size={16} fill="currentColor" strokeWidth={0} />
+                <Star size={16} fill="currentColor" strokeWidth={0} />
+                <Star size={16} fill="currentColor" strokeWidth={0} />
+                <Star size={16} fill="currentColor" strokeWidth={0} />
+                <Star size={16} fill="currentColor" strokeWidth={0} />
+              </span>
+              <span className="hero-trust__rating-text">
+                <strong>4.9/5</strong> from local business owners
+              </span>
+            </div>
+            <ul className="hero-trust__points">
+              <li><Check size={15} strokeWidth={3} /> Free 60-second audit</li>
+              <li><Check size={15} strokeWidth={3} /> No credit card required</li>
+            </ul>
+          </div>
         </div>
 
         <div className="hero-visual reveal" ref={visualRef}>
